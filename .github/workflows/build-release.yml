name: Build Release APK

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Verify Gradle Wrapper
        run: |
          if [ ! -f "gradle/wrapper/gradle-wrapper.jar" ]; then
            echo "Downloading gradle-wrapper.jar..."
            curl -L https://raw.githubusercontent.com/gradle/gradle/v8.2.0/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar
          fi
          chmod +x gradlew

      - name: Download Vosk Models
        run: |
          mkdir -p app/src/main/assets/vosk_models
          cd app/src/main/assets/vosk_models
          
          # Download and extract English model
          echo "Downloading English Vosk model..."
          curl -L -o en_model.zip "https://alphacephei.com/vosk/models/vosk-model-small-en-us-0.15.zip"
          unzip -q en_model.zip
          # Keep the directory structure as extracted
          rm en_model.zip
          
          # Download and extract Persian model
          echo "Downloading Persian Vosk model..."
          curl -L -o fa_model.zip "https://alphacephei.com/vosk/models/vosk-model-small-fa-0.22.zip"
          unzip -q fa_model.zip
          rm fa_model.zip
          
          echo "Vosk models downloaded successfully"
          ls -la
          echo "Checking model structure:"
          find . -name "am" -type f | head -5

      - name: Clean build
        run: ./gradlew clean

      - name: Build Release APK
        run: ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: app/build/outputs/apk/release/app-release.apk
          retention-days: 30
